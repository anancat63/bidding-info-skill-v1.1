---
name: 乌海政府采购商机查询
description: 查询内蒙古政府采购网乌海市招标公告和采购意向，支持按公司业务范围AI评分排序。触发词：招标公告、采购意向、政府采购、商机、投标、标书、乌海招标、有什么标、能做什么项目。
metadata:
  openclaw:
    requires:
      bins: ["python3"]
---

# 乌海市政府采购商机查询

查询内蒙古政府采购网乌海市的招标公告和采购意向公开信息。

## 何时使用

当用户询问以下内容时使用本技能：
- 乌海最近有什么招标公告/采购信息
- 有没有适合我们公司的项目/商机
- 查一下采购意向/招标公告
- 按匹配度/业务范围排序推荐项目

## 四种查询模式

根据用户意图选择对应脚本，用 `exec` 执行：

| 用户意图 | 执行命令 |
|---------|---------|
| 查招标公告，不排序 | `python3 {baseDir}/scripts/bids.py` |
| 查招标公告，按业务匹配排序 | `python3 {baseDir}/scripts/bids_ranked.py` |
| 查采购意向，不排序 | `python3 {baseDir}/scripts/intentions.py` |
| 查采购意向，按业务匹配排序 | `python3 {baseDir}/scripts/intentions_ranked.py` |

可选参数：`--top N`（只取前N条）、`--page-size N`（默认20）

示例：
- 用户说"查前10个招标公告" → `python3 {baseDir}/scripts/bids.py --top 10`
- 用户说"有什么能做的标" → `python3 {baseDir}/scripts/bids_ranked.py`
- 用户说"最近采购意向" → `python3 {baseDir}/scripts/intentions.py`
- 用户说"采购意向按匹配度排" → `python3 {baseDir}/scripts/intentions_ranked.py`

## 结果处理

### 不排序版本（bids.py / intentions.py）
脚本输出JSON数据。直接整理为Markdown表格输出：
- 招标公告表格列：序号 | 标题 | 地区 | 方式 | 截止时间 | 剩余天数 | 链接
- 采购意向表格列：序号 | 标题 | 地区 | 采购单位 | 预计采购时间 | 预算(万元) | 链接
- 剩余天数≤3天标注🔴紧急，≤7天标注🟡关注
- 已截止/已过期的项目脚本已自动过滤，不会出现

### 排序版本（bids_ranked.py / intentions_ranked.py）
脚本输出包含公司业务范围、评分规则和项目编号列表。你需要：
1. 仔细阅读公司业务范围
2. 根据每个项目的名称/描述与公司业务的匹配度，逐项打分0-100
3. 按分数从高到低排序
4. 输出Markdown表格，包含：评分 | 标题 | 地区 | 截止/预计时间 | 评分理由（一句话）| 链接
5. 最后简要总结：高匹配（≥80分）有几个、最近截止的有哪些

评分要点：
- 信息化、数字化、智慧类、系统集成、软件开发、人工智能、大数据、物联网、网络安全、视频监控等方向是核心业务，给高分
- 建筑维修、操场改造、医疗设备、家具采购等与信息化无关的给低分
- 注意识别标题党：标题含相关词但实际内容不相关的要给低分
- 评分要客观，不要所有项目都给高分

## 意图不明确时

如果用户没有说明查招标公告还是采购意向、要不要排序，不要猜测，直接询问：

> 我可以帮你查询乌海市政府采购信息，请确认：
> 1. 查询类型：① 招标公告 ② 采购意向
> 2. 是否需要按公司业务匹配度排序？

## 能力范围

- 仅支持内蒙古自治区政府采购网（ccgp-neimenggu.gov.cn）
- 仅支持乌海市地区（regionCode=150301）
- 不支持其他省份、其他盟市
- 不支持关键词搜索，只能按列表顺序获取
- 数据为实时采集，如遇网络错误请告知用户稍后重试
